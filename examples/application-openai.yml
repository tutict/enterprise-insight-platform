server:
  port: 8090

spring:
  application:
    name: harness-compiler-platform

llm:
  model-type: remote
  api-key: ${OPENAI_API_KEY}
  base-url: https://api.openai.com/v1
  model: gpt-4o-mini
  timeout: 2m
